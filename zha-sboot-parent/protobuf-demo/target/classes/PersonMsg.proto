syntax = "proto3";

package com.zw.model;

option java_outer_classname = "PersonMsg";

message Person {

    // ID（必需）
    int32 id = 1;

    // 姓名（必需）
    string name = 2;

    // email（可选）
    string email = 3;

    // 朋友（集合）
    repeated string friends = 4;
}